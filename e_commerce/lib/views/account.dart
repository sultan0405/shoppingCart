import 'package:flutter/material.dart';

// TODO: SAHYPALANY ATLARYNA PAGE/SCREEN KOSHUP GITSANG OZUNGA ANGSAT BOLADY.
// MASALAM ACCOUNT DAGAN MODEL,COMPONENT VA PAGE BOLUP BILADI.
class Account extends StatelessWidget {
  const Account({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Account"),
    );
  }
}
